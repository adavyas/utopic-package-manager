{
  "name": "utopic-package-manager",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:chat": "tsc -p tsconfig.json",
    "check:chat": "tsc -p tsconfig.json --noEmit && npm run build:chat && git diff --exit-code -- python/utopic/node/utopic-chat.js && node --check python/utopic/node/utopic-chat.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
